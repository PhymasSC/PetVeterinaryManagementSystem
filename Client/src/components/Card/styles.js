import styled from "styled-components";
import tw from "twin.macro";

const Card = styled.div`
    ${tw`w-full mx-auto my-4 bg-white shadow-md rounded-xl p-8`}
`;

const CardPie = styled.div`
    ${tw`w-full mx-auto my-4 bg-white shadow-md rounded-xl p-8`}
`;

export default Card;
export {CardPie};