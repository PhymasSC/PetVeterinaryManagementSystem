import styled from "styled-components";
import tw from "twin.macro";

const ClockLabel = styled.div`
	${tw`flex gap-2 justify-center items-center bg-white px-2 py-1 rounded-xl text-primary-700`}
`;

export default ClockLabel;
